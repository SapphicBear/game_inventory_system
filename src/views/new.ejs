<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head.ejs"); %>
</head>
<body>
    <div class="wrapper new">
        <header>
            <%- include("./partials/header.ejs"); %>
        </header>
        <main class="new">
            <form action="/new" method="POST" class="new-form">
                <fieldset>
                        <legend>Item information</legend>
                <div class="new form-row">
                    <label for="name">Title: </label>
                    <input type="text" name="name" id="name" required>
                </div>
                <div class="new form-row">
                    <label for="genre">Genre: </label>
                    <input type="text" name="genre" id="genre" required>
                </div>
                <div class="new form-row">
                    <label for="studio">Game Studio: </label>
                    <select name="studio" id="studio">
                        <option value="" selected disabled hidden>Select a Studio</option>
                        <% studios.map((studio) => { %>
                            <option value="<%= studio.name %>"><%= studio.name %></option>
                        <% }); %>
                    </select>
                </div>
                <div class="new form-row">
                    <label for="release_year">Release year: </label>
                    <input type="text" name="release_year" id="release_year" required>
                </div>
                </fieldset>
                <fieldset>
                    <legend>Store information</legend>
                <div class="new form-row">
                    <label for="console">Available on: </label>
                    <select name="console" id="console">
                        <option value="" selected disabled hidden>Select a Console</option>
                        <% consoles.map((console) => { %>
                            <option value="<%= console.name %>"><%= console.name %></option>
                        <% }); %>
                    </select>
                    <!-- TODO: add multi select for consoles, can remove existing ones -->
                </div>
                <div class="new form-row">
                    <label for="in_stock">In Stock: </label>
                    <input type="number" name="in_stock" id="in_stock" placeholder="0" required>
                </div>
                <div class="new form-row">
                    <label for="price">Price: </label>
                    <input type="tel" name="price" id="price" placeholder="0.00" required>
                </div>
                </fieldset>
                <button type="submit">Submit</button>
                <a class="button" href="/">Cancel</a>
            </form>
            <aside>
                <ul>
                    <% if (errors) { %>
                <% errors.map((error) => { %>
                    <li><%= error.msg %></li>
                <% }); %>
                <%}%>
                </ul>
        </aside>
        </main>
        
        <footer>
            <%- include("./partials/footer.ejs", { links: links }); %>
        </footer>
    </div>
</body>
</html>